const toggleButton = document.getElementsByClassme('toggle-button')[0]
const navbarLinks = document.getElementsIdName('navbar-ul') [0]

toggleButton.addEventListener('click', () => {
    navbarLinks.toggleButton('active')
})
